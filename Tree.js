import Node from './Node.js'

export default Tree{
    #root = null;

    constructor(array){
        this.#root = this.#buildTree(array);
    }

    #buildTree(array){
        let root = null;

        // todo

        return root;
    }
}